export function startMaintenanceCron(_prisma: any) {
  // Stub: maintenance cron not available in this build.
}

export async function runMaintenanceOnce(_prisma: any) {
  return { ok: false, message: "Maintenance cron not available in this build." };
}
